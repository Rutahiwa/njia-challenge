[project]
name = "njia-agent-challenge"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.70",
    "fastapi>=0.115",
    "httpx>=0.27",
    "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = ["pytest>=8.0"]
